# RAG系统使用指南（简化版）

## 系统说明

由于网络问题无法安装pandas和ChromaDB，我创建了简化版RAG系统，使用Python内置的csv模块和关键词搜索，无需额外依赖。

## 已加载的数据

- **上海**：100个景点
- **北京**：90个景点
- **天津**：80个景点
- **总计**：270个景点

## 使用方法

### 步骤1：初始化数据库

```bash
python scripts/init_attraction_db_simple.py
```

预期输出：
```
============================================================
初始化景点数据库（简化版）
============================================================

1. 加载景点数据...
加载 上海 的景点数据：100 个景点
加载 北京 的景点数据：90 个景点
加载 天津 的景点数据：80 个景点
   加载了 3 个城市的数据：上海, 北京, 天津

2. 创建文档存储...
创建文档存储...
文档存储创建完成，文档数量：270

3. 测试搜索...

   测试查询：故宫 (城市：北京)
   找到 2 个结果
   1. 故宫珍宝馆The Antiquarium of The Palace Museum
   2. 故宫博物院The Palace Museum

   测试查询：历史景点 (城市：北京)
   找到 2 个结果
   1. 故宫博物院The Palace Museum
   2. 八达岭长城Badaling Great Wall

   测试查询：适合家庭游玩 (城市：上海)
   找到 2 个结果
   1. 上海迪士尼度假区Shanghai Disney Resort
   2. 上海海洋水族馆Shanghai Ocean Aquarium

============================================================
初始化完成！
============================================================
```

### 步骤2：启动应用

```bash
python app.py
```

### 步骤3：测试查询

在前端输入以下问题测试：

#### 1. 搜索景点
- "北京有什么历史景点？"
- "推荐上海适合家庭游玩的景点"
- "天津有什么好玩的？"
- "北京有什么博物馆？"

#### 2. 查询景点详情
- "故宫的开放时间是什么？"
- "八达岭长城的门票价格"
- "外滩的介绍"

#### 3. 列出景点
- "列出北京的所有景点"
- "上海有哪些景点？"

## 搜索功能说明

### 1. 精确搜索
输入景点名称，会返回匹配的景点：
- "故宫" → 返回故宫相关景点
- "外滩" → 返回外滩相关景点

### 2. 语义搜索
支持语义关键词，会返回相关类型的景点：
- "历史景点" → 返回历史、文化、古迹类景点
- "适合家庭游玩" → 返回适合家庭、亲子、儿童类景点
- "免费景点" → 返回免费的景点
- "自然景点" → 返回公园、山水、风景类景点

### 3. 城市过滤
可以指定城市进行搜索：
- "北京的历史景点" → 只搜索北京的历史景点
- "上海的博物馆" → 只搜索上海的博物馆

## 系统架构

```
用户提问
    ↓
主协调Agent
    ↓
选择工具
    ↓
search_attraction_local / get_attraction_details_local / list_attractions_local
    ↓
关键词搜索（简化版）
    ↓
返回相关景点信息
    ↓
LLM生成回答
    ↓
返回给用户
```

## 核心特性

### 1. 无需额外依赖
- 使用Python内置的csv模块
- 使用关键词搜索代替向量检索
- 无需安装pandas、chromadb

### 2. 智能匹配
- 精确匹配：景点名称完全匹配
- 关键词匹配：支持多关键词搜索
- 语义匹配：理解"历史"、"家庭"、"免费"等概念

### 3. 评分排序
- 根据匹配分数排序结果
- 返回最相关的景点

### 4. 优先级策略
Agent优先使用本地数据：
1. 本地CSV数据（优先）
2. 高德API（备选）

## 文件说明

### 创建的文件
- `src/agent/attraction_loader_simple.py` - 简化版数据加载器
- `src/agent/attraction_vector_store_simple.py` - 简化版检索模块
- `scripts/init_attraction_db_simple.py` - 简化版初始化脚本

### 修改的文件
- `src/agent/tools.py` - 使用简化版模块
- `src/agent/travel_agent.py` - 添加了新的工具

## 扩展方法

要添加新的城市，只需：

1. 在jingdian目录下添加新的CSV文件（如`广州.csv`）
2. 运行初始化脚本：
   ```bash
   python scripts/init_attraction_db_simple.py
   ```
3. 系统会自动加载新数据

## 注意事项

1. **首次运行**：必须先运行初始化脚本
2. **数据更新**：如果CSV文件更新，需要重新运行初始化脚本
3. **API密钥**：仍需配置OPENAI_API_KEY用于LLM生成
4. **搜索限制**：简化版使用关键词搜索，不如向量检索精确

## 与完整版的区别

| 特性 | 简化版 | 完整版 |
|------|---------|---------|
| 依赖 | 无额外依赖 | pandas, chromadb, tiktoken |
| 搜索方式 | 关键词匹配 | 向量检索 |
| 语义理解 | 基于关键词映射 | 基于向量相似度 |
| 安装难度 | 简单 | 需要额外依赖 |
| 搜索精度 | 中等 | 较高 |

## 技术栈

- **数据处理**：Python内置csv模块
- **搜索算法**：关键词匹配 + 语义映射
- **Agent框架**：LangChain
- **LLM**：OpenAI API

## 常见问题

### Q1: 搜索结果不准确怎么办？

**A**: 简化版使用关键词搜索，可以尝试：
- 使用更具体的关键词（如"故宫博物院"而不是"故宫"）
- 使用语义关键词（如"历史"、"博物馆"）
- 指定城市进行搜索

### Q2: 如何升级到完整版？

**A**: 当网络问题解决后，可以：
1. 安装pandas、chromadb、tiktoken
2. 使用完整版模块替换简化版
3. 重新运行初始化脚本

### Q3: 支持哪些语义关键词？

**A**: 当前支持的语义关键词：
- 历史：历史、文化、古迹、博物馆、宫殿、寺庙、古建筑
- 家庭：家庭、亲子、儿童、小孩、孩子、适合家庭、亲子游
- 免费：免费、0元、不需要门票
- 自然：自然、公园、山水、风景、景观
- 美食：美食、小吃、特色菜、餐厅

可以在`attraction_vector_store_simple.py`中扩展更多语义关键词。
